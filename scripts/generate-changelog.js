#!/usr/bin/env node

import { promises as fs } from "node:fs";
import path from "node:path";
import { execSync } from "node:child_process";

// Check if two arguments are provided
if (process.argv.length !== 4) {
	console.error("Usage: node generate-changelog.js <repo_url> <file_path>");
	process.exit(1);
}

// Assign arguments to variables
const url = process.argv[2];
const filePath = process.argv[3];

// Check if changelog-from-release is installed
function checkChangelogFromRelease() {
	try {
		// Try to run the command to see if it exists
		execSync("which changelog-from-release", { stdio: "pipe" });
	} catch (error) {
		console.error("Error: changelog-from-release is not installed.");
		console.error(
			"Please install it from: https://github.com/rhysd/changelog-from-release",
		);
		process.exit(1);
	}
}

async function generateChangelog() {
	try {
		// Create the changelog.mdx file with the specified content
		const frontmatter = `---
title: Changelog
---

`;

		// Run changelog-from-release and get output
		const changelogOutput = execSync(`changelog-from-release -r "${url}"`, {
			encoding: "utf8",
			stdio: "pipe",
		});

		// Remove the generated tag at the end
		let cleanOutput = changelogOutput
			.replace(/<!-- Generated by .* -->\s*$/, "")
			.trim();

		// Escape bare < that aren't HTML/JSX tag starts (e.g. "<4.6", "<3 with love")
		// to prevent MDX parse errors. Skip code blocks and inline code so backticks
		// like `<4.6 to >=4.6` stay verbatim.
		const ltSplit = cleanOutput.split(/(```[\s\S]*?```|`[^`\n]*`)/g);
		cleanOutput = ltSplit
			.map((part, idx) =>
				idx % 2 === 1 ? part : part.replace(/(<)(\s+\w|\d)/g, "&lt;$2"),
			)
			.join("");

		// Skip when there are no releases (e.g. repos like community-contracts
		// that don't tag releases) — emitting a frontmatter-only file would
		// surface a useless empty page in the docs site.
		if (!cleanOutput) {
			console.log(
				`No releases found for ${url}; skipping changelog write.`,
			);
			return;
		}

		// Write the complete content
		const fullContent = frontmatter + cleanOutput + "\n";
		await fs.writeFile(
			path.join(filePath, "changelog.mdx"),
			fullContent,
			"utf8",
		);

		console.log(
			`Changelog generated successfully at ${path.join(filePath, "changelog.mdx")}`,
		);
	} catch (error) {
		console.error("Error generating changelog:", error.message);
		process.exit(1);
	}
}

// Check if required tool is installed
checkChangelogFromRelease();

generateChangelog();
