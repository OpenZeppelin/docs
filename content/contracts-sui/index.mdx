---
title: Contracts for Sui
---

import { latestStable } from "./latest-versions.js";

**OpenZeppelin Contracts for Sui** is a Move library for building secure applications on Sui with secure and efficient primitives.

## Getting Started

<Cards>
  <Card href={`/contracts-sui/${latestStable}`} title={`Latest Stable (${latestStable})`}>
    Start from the stable docs set for the current Sui release.
  </Card>
  <Card href={`/contracts-sui/${latestStable}/quickstart`} title="Quickstart">
    Set up a Move package, install OpenZeppelin dependencies, and run your first build.
  </Card>
  <Card href={`/contracts-sui/${latestStable}/integration`} title="Integration">
    Integrate ownership transfer policies and math utilities into production flows.
  </Card>
</Cards>

## Packages

<Cards>
  <Card href={`/contracts-sui/${latestStable}/math/core`} title="openzeppelin_math">
    Overflow-aware arithmetic, configurable rounding, decimal scaling, and wide-integer helpers from the `openzeppelin_math` package.
  </Card>
  <Card href={`/contracts-sui/${latestStable}/contracts/access`} title="openzeppelin_access">
    Two-step and delayed ownership transfer wrappers from the `openzeppelin_access` package for capabilities and privileged objects.
  </Card>
</Cards>
