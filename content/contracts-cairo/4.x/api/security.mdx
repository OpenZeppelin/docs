---
title: Security
---

import { API_SOURCE_VERSION } from "../utils/constants.js";

This crate provides components to handle common security-related tasks.

## Initializable [#initializable]

### `InitializableComponent` [toc] [#InitializableComponent]

<APIGithubLinkHeader moduleName="InitializableComponent" link={`https://github.com/OpenZeppelin/cairo-contracts/blob/${API_SOURCE_VERSION}/packages/security/src/initializable.cairo`} />

```rust
use openzeppelin_security::InitializableComponent;
```

Component enabling one-time initialization for contracts.

Embeddable Implementations

InitializableImpl

- [`is_initialized(self)`](#InitializableComponent-is_initialized)

Internal Implementations

InternalImpl

- [`initialize(self)`](#InitializableComponent-initialize)

#### Embeddable functions [!toc] [#InitializableComponent-Embeddable-Functions]

<APIItem
  functionSignature="is_initialized(self: @ComponentState) → bool"
  id="InitializableComponent-is_initialized"
  kind="external"
>
Returns whether the contract has been initialized.
</APIItem>

#### Internal functions [!toc] [#InitializableComponent-Internal-Functions]

<APIItem
  functionSignature="initialize(ref self: ComponentState)"
  id="InitializableComponent-initialize"
  kind="internal"
>
Initializes the contract. Can only be called once.

Requirements:

- the contract must not have been initialized before.
</APIItem>

## Pausable [#pausable]

### `PausableComponent` [toc] [#PausableComponent]

<APIGithubLinkHeader moduleName="PausableComponent" link={`https://github.com/OpenZeppelin/cairo-contracts/blob/${API_SOURCE_VERSION}/packages/security/src/pausable.cairo`} />

```rust
use openzeppelin_security::PausableComponent;
```

Component to implement an emergency stop mechanism.

Embeddable Implementations

PausableImpl

- [`is_paused(self)`](#PausableComponent-is_paused)

Internal Implementations

InternalImpl

- [`assert_not_paused(self)`](#PausableComponent-assert_not_paused)
- [`assert_paused(self)`](#PausableComponent-assert_paused)
- [`pause(self)`](#PausableComponent-pause)
- [`unpause(self)`](#PausableComponent-unpause)

Events

- [`Paused(account)`](#PausableComponent-Paused)
- [`Unpaused(account)`](#PausableComponent-Unpaused)

#### Embeddable functions [!toc] [#PausableComponent-Embeddable-Functions]

<APIItem
  functionSignature="is_paused(self: @ComponentState) → bool"
  id="PausableComponent-is_paused"
  kind="external"
>
Returns whether the contract is currently paused.
</APIItem>

#### Internal functions [!toc] [#PausableComponent-Internal-Functions]

<APIItem
  functionSignature="assert_not_paused(self: @ComponentState)"
  id="PausableComponent-assert_not_paused"
  kind="internal"
>
Panics if the contract is paused.
</APIItem>

<APIItem
  functionSignature="assert_paused(self: @ComponentState)"
  id="PausableComponent-assert_paused"
  kind="internal"
>
Panics if the contract is not paused.
</APIItem>

<APIItem
  functionSignature="pause(ref self: ComponentState)"
  id="PausableComponent-pause"
  kind="internal"
>
Pauses the contract.

Requirements:

- the contract must not be paused.

Emits a [Paused](#PausableComponent-Paused) event.
</APIItem>

<APIItem
  functionSignature="unpause(ref self: ComponentState)"
  id="PausableComponent-unpause"
  kind="internal"
>
Unpauses the contract.

Requirements:

- the contract must be paused.

Emits an [Unpaused](#PausableComponent-Unpaused) event.
</APIItem>

#### Events [!toc] [#PausableComponent-Events]

<APIItem
  functionSignature="Paused(account: ContractAddress)"
  id="PausableComponent-Paused"
  kind="event"
>
Emitted when the contract is paused by `account`.
</APIItem>

<APIItem
  functionSignature="Unpaused(account: ContractAddress)"
  id="PausableComponent-Unpaused"
  kind="event"
>
Emitted when the contract is unpaused by `account`.
</APIItem>

## ReentrancyGuard [#reentrancyguard]

### `ReentrancyGuardComponent` [toc] [#ReentrancyGuardComponent]

<APIGithubLinkHeader moduleName="ReentrancyGuardComponent" link={`https://github.com/OpenZeppelin/cairo-contracts/blob/${API_SOURCE_VERSION}/packages/security/src/reentrancyguard.cairo`} />

```rust
use openzeppelin_security::ReentrancyGuardComponent;
```

Component to help prevent reentrant calls.

Internal Implementations

InternalImpl

- [`start(self)`](#ReentrancyGuardComponent-start)
- [`end(self)`](#ReentrancyGuardComponent-end)

#### Internal functions [!toc] [#ReentrancyGuardComponent-Internal-Functions]

<APIItem
  functionSignature="start(ref self: ComponentState)"
  id="ReentrancyGuardComponent-start"
  kind="internal"
>
Prevents a contract's function from calling itself or another protected function, directly or indirectly.

Requirements:

- the guard must not be currently enabled.
</APIItem>

<APIItem
  functionSignature="end(ref self: ComponentState)"
  id="ReentrancyGuardComponent-end"
  kind="internal"
>
Removes the reentrant guard.
</APIItem>
